import { BookOpen } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function Resources() {
  return (
    <PlaceholderPage
      title="Resource Vault"
      description="Access templates, guides, and proven frameworks for clinic marketing. Your resource library is being built."
      icon={BookOpen}
    />
  );
}
